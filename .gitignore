# Rust / Cargo
/target
**/*.rs.bk
Cargo.lock.orig

# Env / secrets
.env
.env.*
!.env.example
# External config loader (issue #34) — aiter-server init writes this; it can
# hold real Razorpay secrets, so never commit it.
aiter.env

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Logs
*.log
